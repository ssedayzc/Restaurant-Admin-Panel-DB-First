@*@using DinoRestaurant.Models.Entity
    @model List<Categories>*@
@{
    ViewBag.Title = "AddCategory";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@*<h1>Yeni Kategori Ekleme Sayfası</h1>
    <br />
    <br>

    @using (Html.BeginForm("AddCategory", "Category", FormMethod.Post))
    {
        <div class="form-group">
            @Html.Label("Kategori Adı")
            @Html.TextBoxFor(x => x.CategoryName, new { @class = "form-control" })
            <br /> <br />
            @Html.Label("Açıklamayı Yazınız")
            @Html.TextAreaFor(x => x.Description, new { @class = "form-control" })
            <br /> <br />
            @Html.Label("Kategori Durumunu Seçiniz")
            <div class="checkbox">
                <label>@Html.CheckBoxFor(x => x.IsActive) Aktif</label>
            </div>
            <br /><br />
            <button type="submit" class="btn btn-info">Kaydet</button>
        </div>
    }*@

<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-header">
                <strong> Yeni Kategori Ekleme Sayfası </strong>
            </div>
            <div class="card-body card-block">
                <form asp-controller="Category" asp-action="AddCategory" method="post" enctype="multipart/form-data" class="form-horizontal">
                    @*<label><strong>ID</strong></label>
        <input type="text" class="form-control" name="CategoryId" />*@
                    <br />
                    <label><strong>Kategori Görseli</strong></label>
                    <input type="file" class="form-control" name="Image" />
                    <br />
                    <label><strong>Kategori Adı</strong></label>
                    <input type="text" class="form-control" name="CategoryName" />
                    <br />
                    <label><strong>Kategori Açıklaması</strong></label>
                    <input type="text" class="form-control" name="Description" />
                    <br />
                    <label><strong>Kategori Durumu</strong></label><br />
                    <div class="form-check-inline">
                        <input type="checkbox" class="form-check-input" name="IsActive" id="isActiveCheckbox" value="true" />
                        <label class="form-check-label" for="isActiveCheckbox">Aktif</label>
                    </div>
                    <div class="form-check-inline">
                        <input type="checkbox" class="form-check-input" name="IsActive" value="false" />
                        <label class="form-check-label">Pasif</label>
                    </div>
                    <br /><br />
                    <button class="btn btn-primary btn-sm" type="submit">
                        <i class="fa fa-dot-circle-o"></i>Kategori Ekle
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
